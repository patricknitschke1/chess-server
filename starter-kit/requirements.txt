# The starter kit is not standalone: it imports `chess_core` from this
# repository, so install the repository rather than this file.
#
#     python3 -m venv .venv          # from the repository root
#     .venv/bin/python -m pip install -e .
#
# That installs everything the kit needs, including the two direct
# dependencies listed below, which are recorded here for reference only.
chess>=1.11
httpx>=0.27
